{
  "name": "veyra",
  "version": "0.1.0",
  "private": true,
  "description": "Developer-first AI platform for planning, building, testing, deploying, and operating software systems",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "clean": "turbo run clean",
    "format": "prettier --write \"**/*.{ts,tsx,md,json}\"",
    "typecheck": "turbo run typecheck",
    "build:native": "npm run build:native --workspace=@veyra/web",
    "desktop:dev": "npm run dev --workspace=@veyra/desktop",
    "desktop:dist": "npm run dist:win --workspace=@veyra/desktop",
    "mobile:apk": "npm run android:debug --workspace=@veyra/mobile"
  },
  "devDependencies": {
    "7zip-bin": "^5.2.0",
    "prettier": "^3.2.0",
    "turbo": "^1.12.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "packageManager": "npm@9.0.0"
}
